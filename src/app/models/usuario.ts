export interface Usuario {
    uid: string | any ; // id para autentificacion de Firebase
    nombre1: string // -> nombre
    email: string; // -> email
    rol: string; // -> roles
    contrasena: string;
}
